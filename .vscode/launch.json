{
  "version": "0.2.0",
  "configurations": [{
    "name": "Launch Chrome",
    "preLaunchTask": "Install & Serve",
    "request": "launch",
    "type": "chrome",
    "url": "http://localhost:3000",
    "webRoot": "${workspaceFolder}"
  }]
}
